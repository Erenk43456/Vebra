model:
  vocab_size: 284
  hidden_size: 64
  num_layers: 2
  num_heads: 8
  intermediate_size: 256
  max_sequence_length: 16

tokenizer:
  path: tokenizer.json
  vocab_size: 284
  min_frequency: 2

training:
  batch_size: 4
  learning_rate: 0.001
  steps: 10
  device: cpu
  precision: fp32